{
  "name": "idle-adventure-backend",
  "version": "1.0.0",
  "description": "Idle adventure game backend with progression and monetization",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply idle-adventure-db",
    "generate": "wrangler d1 migrations create",
    "build": "tsc"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240124.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.19.0"
  }
}

